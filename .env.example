# Example environment variables (DO NOT COMMIT real secrets)
# Copy this to `.env` locally and fill with real values.

# Primary database URL (tenant/management DB). Example format:
# DATABASE_URL="postgresql://<user>:<password>@<host>:5432/<db>"
DATABASE_URL="postgresql://postgres:REPLACE_ME@db.example.com:5432/phc_hms"

# JWT secret used by all services. Use a strong random value in production.
JWT_SECRET="REPLACE_WITH_STRONG_JWT_SECRET"

# Supabase service role key (if used server-side)
SUPABASE_SERVICE_ROLE_KEY="REPLACE_WITH_SUPABASE_SERVICE_ROLE_KEY"

# Optional: Vercel deployment values (for CLI use or CI)
# VERCEL_TOKEN and VERCEL_* are used by the GitHub Actions workflow for frontend.
VERCEL_TOKEN=""
VERCEL_ORG_ID=""
VERCEL_PROJECT_ID=""

# Azure publish profiles will be set as GitHub secrets named in the workflows
# AZURE_APP_*_PUBLISH_PROFILE secrets should contain the base64 publish profile content.
