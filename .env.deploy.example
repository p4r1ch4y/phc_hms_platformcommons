# Example .env.deploy - copy to .env.deploy and fill values before running the deploy script
# Common environment variables used by services
DATABASE_URL=postgresql://username:password@your-db-host:5432/phc_hms
JWT_SECRET=replace_with_secure_jwt_secret
# Optional settings
# SMTP_URL=smtp://user:pass@smtp.example.com:587
# STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...;
# Any other service envs can be added here
